/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['www.vectorlabz.com','www.logoaspire.com','flagcdn.com'],
        
      },
};

export default nextConfig;
